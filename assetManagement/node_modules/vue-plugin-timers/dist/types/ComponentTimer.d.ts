/// <reference types="node" />
import { Vue } from 'vue/types/vue';
export declare type TimerState = 'created' | 'running' | 'expired' | 'stopped';
export default class ComponentTimer {
    repeat: boolean;
    interval: number;
    timerId: NodeJS.Timer | number;
    vm: Vue;
    methodName: string;
    state: TimerState;
    method: Function;
    args?: () => [];
    start(): void;
    stop(): void;
    constructor(methodName: string, interval?: number, repeat?: boolean, args?: <V extends Vue>(this: V) => []);
    setVM(vm: Vue): void;
}
